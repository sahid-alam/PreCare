import type { RedFlagCategory } from "@/lib/types";

export function detectRedFlags(transcript: string): RedFlagCategory[] {
  // TODO: implement keyword scanner — see docs/red-flags.md
  void transcript;
  return [];
}
